[project]
name = "do-anything"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "docker>=7.1.0",
]

[dependency-groups]
dev = [
    "ipykernel>=6.29.5",
]
